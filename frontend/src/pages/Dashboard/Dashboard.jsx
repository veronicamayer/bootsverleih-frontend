import Navigation from "../../components/Navigation/Navigation";

const Dashboard = () => {
    return (
        <section id="dashboard">
            <Navigation />
            <h1>Dashboard</h1>
        </section>
    );
};

export default Dashboard;
